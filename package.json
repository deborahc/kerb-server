{
  "name": "kerb-server",
  "description": "Proxy server to accept Kerberos tickets for XVM service",

  "engines": {
    "node": ">= 0.10.30",
    "npm": ">= 1.0.0"
  },

  "repository": {
  "type": "git",
  "private": "true"
  },

  "dependencies": {
    "express": ">= 4.0.0",
    "body-parser":"*"
  }
}
